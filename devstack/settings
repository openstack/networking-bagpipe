NETWORKING_BAGPIPE_DIR=$DEST/networking-bagpipe
BAGPIPE_AGENT=bagpipe-linuxbridge
BAGPIPE_AGENT_BINARY="$NEUTRON_BIN_DIR/neutron-bagpipe-linuxbridge-agent"

# agent settings
Q_AGENT=${Q_AGENT:-${BAGPIPE_AGENT}}

